export class Lei {
  id: number;
  descricao: string;
  sumula: string;

  constructor(id: number, descricao: string, sumula: string) {
    this.id = id;
    this.descricao = descricao;
    this.sumula = sumula;
  }
}
